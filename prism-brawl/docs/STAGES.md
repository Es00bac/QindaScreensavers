# Prism Brawl — seven arenas

The stage update adds four environments and rebuilds the original three. It
ships with the full-body animation update described in [ANIMATION.md](ANIMATION.md).
All environment models and skies are generated by C++ and GLSL at runtime;
the wallpapers are art-direction references, with no external image dependency.

## Reference mapping

The actual local images were inspected on September 19, 2026. The official
installed collection documents its provenance in
`/usr/share/qindaqt/wallpapers/ARTWORK.md` and includes Pengu and Ducké.

| Command key | Arena | References and resulting scenery |
|---|---|---|
| `prism` | Prism Terminal | `~/Downloads/image-gen-1(6).png`: obsidian decks, a mint-rimmed eclipse, copper details, orbital gate and dark crystal spires. |
| `garden` | Reactor Garden | The jade technology/nature palette of official `qinda-bliss.png`: circuit-tree foliage, hydroponic beds and a nocturnal greenhouse interpretation. |
| `rooftop` | Afterglow Rooftop | `~/Downloads/image-gen-4(6).png` and official `qinda-punk.png`: layered neon skyline, rooftop HVAC, cables, passing traffic and rain. |
| `bliss` | Bliss Circuit — new | `~/Downloads/image-gen-3(6).png` and official `qinda-bliss.png`: rolling sunlit hills, reclaimed boards and computers, flowers, old dishes and a distant city. |
| `compile` | Compile Club — new | Official `compile-club.png`: warm articulated lamps, workbenches, jade terminals, server racks, cables, steaming coffee and Ducké's rotating desk fan. |
| `aurora` | Aurora Glacier — new | `~/Downloads/image-gen-5(3).png`: icy lake, mountain horizon, crystal formations, moon and slowly moving aurora curtains. |
| `azure` | Azure Fold — new | `~/Downloads/image-gen-2(6).png`: broad blue/cyan mineral ribbons, polished porcelain decks, floating stones and soft blue light. |

Official paths above are relative to `/usr/share/qindaqt/wallpapers/`.
These are authored interpretations, not image projections or copies of Nintendo
stages. The original wallpaper files are not redistributed.

## Motion and platform behavior

Each arena has one main deck and three one-way upper platforms. Visible deck
tops share their exact width, position and height with collision surfaces.
Garden has sliding side decks; Compile has a central vertical lift; Aurora has
opposing side drifts; Azure has a gliding top deck. Fighters and pickups ride
moving platforms. Bliss has an asymmetric stepped layout; Prism and Rooftop
retain their stable layouts.

Rain, traffic, motes, foliage, terminal cursors, fan blades, steam, aurora curtains
and mineral ribbons move continuously. Lighting, sky, platform undersides and
surface materials vary by arena. Wet metal and ice use stylized procedural
highlights and streaks; there is no real-time planar reflection pass.

Since 1.2.1, `--stage auto` shuffles all seven arenas, showing each once before
reshuffling, with the existing fade between rounds. The first stage of a new
shuffle cannot match the previous round. Normal unseeded launches remember
their starting arena and exclude it from the next launch's random choice.
History is a single stage key in
`${XDG_STATE_HOME:-$HOME/.local/state}/prism-brawl/last-starting-stage`.
If that location cannot be written, startup falls back to random selection.
`--seed N` bypasses history for deterministic replays; `--stage NAME` stays
fixed. Offline tools and showcases leave launch history untouched.
`--list-stages` lists keys without opening a window.

```sh
prism-brawl --windowed --stage compile
prism-brawl --all-screens --stage auto
prism-brawl --windowed --stage aurora --reduced-motion --mute
```

## Editable source and export

* `include/battle.hpp`: the single stage registry.
* `src/battle.cpp`: platform layouts, moving-platform physics and rotation.
* `src/startup_stage.cpp`: remembered startup choice and exclusion across launches.
* `src/stages.cpp`: environment geometry and stage palettes.
* `src/meshes.cpp`, `include/scene.hpp`: terrain, foliage, mineral folds and crystals.
* `shaders/sky.frag`, `shaders/mesh.frag`: skies, materials and lighting.

`--export-models assets/models` writes fifteen OBJ/MTL sets: eight posed fighters
and seven platform assemblies. Exports contain static geometry and basic
materials; animation, environment backgrounds and shader effects remain in source.
The optional `tools/render_stages_preview.py` captures all seven scenes and a
21-second, 60 fps arena film using the real renderer.

## Arena release validation (1.2.0)

The release and AddressSanitizer/UndefinedBehaviorSanitizer builds pass all
13 CTest checks. The unit suite checks 1,939,779 assertions, including unique
seven-stage rotation, invalid-stage rejection, finite environment geometry,
exact collision/deck alignment and lift transport for fighters and pickups.
Environment instance counts stay below the tested bound of 3,000.

The deterministic stress sweep covers 96 cases: all seven fixed stages plus
automatic rotation, multiple seeds, and two/four/eight fighters. Across 48
simulated hours it executed 20,736,000 ticks, 146,728 hits, 19,011 knockouts,
26,170 taunts, 104,646 tumbles and 167,416 hard landings without invariant failures.
The stress run tests simulation, not 48 hours of real-time rendering.

All seven stage captures and the arena film use the native OpenGL renderer on
AMD Radeon Graphics (radeonsi / renoir), Mesa 26.1.8, through SDL's X11 backend
at 1280 × 720. GPU screenshots verify shader compilation and presentation;
they do not establish a frame-rate guarantee on other hardware. Native output
lifecycle code is unchanged by the arena update.

Logs and individual frames: `validation/stages/`. The initial release's broader
historical checks are retained in [TESTING.md](TESTING.md).

## Startup and shuffle validation (1.2.1)

All 13 release and AddressSanitizer/UndefinedBehaviorSanitizer CTest checks pass.
The unit suite now checks 1,958,312 assertions, including 64 seeds across twelve
complete shuffled bags each, nonrepeating bag boundaries, fixed-stage overrides,
reproducible seeds and rewinding, and persistent launch history with repeated
seeds, invalid records and unavailable storage.

Thirty separate native launches with isolated state storage encountered all
seven starting arenas with no adjacent repeat. Two explicitly seeded launches
matched exactly; fixed stages, showcases, captures, listing and headless checks
left history untouched. The 96-case stress sweep again completed 48 simulated
hours and 20,736,000 ticks without invariant failures. Logs are retained in
`validation/startup/`.
